---
export const prerender = true;

import BenchmarksPage from "../../../components/pages/BenchmarksPage.astro";

const lang = Astro.currentLocale === "en" || Astro.currentLocale === "es" ? (Astro.currentLocale as "en" | "es") : "en";
---

<BenchmarksPage lang={lang} />
